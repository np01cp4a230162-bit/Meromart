# MeroMart Database Configuration
# ─────────────────────────────────
# Change these values to match your local MySQL setup.
# This file is read by DBConfig.java to establish the JDBC connection.

db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/webapp_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.username=root
db.password=
